-- not started        >> in progress         $$ completed


-- variable changes inside functions are still drawn as "variable" boxes. they should be categorized and function changes

-- some changes inside functions are not categorized correctly and the FILE box is displayed instead of a function box

-- when detecting a change, extract the comments and display them in large fonts above the changed file for 3 seconds

-- the label in a symbol box (e.g. "myFunction() ~") should use larger font size as the box size increases  - make sure the label still fits in the box
