-- not started        >> in progress         $$ completed



-- I want a new mode, similar to realtime changes. It should display the changes as symbols and not as code. For example - new function as a box, new code in a function as a box,
one function calling another as a connector between two boxes. This should visualize the changes to the user is an intuitive way and should make complex changes easily coherent


