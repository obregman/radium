-- not started        >> in progress         $$ completed


Symbol visualization view tasks:

$$ there's still problem with symbol boxes layout. they are sometimes overlap or extend from the file box border
