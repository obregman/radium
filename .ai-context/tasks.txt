-- not started        >> in progress         $$ completed



$$ change the symbol visualization layout. instead of vertical stacking, the layout should be more dynamic. use small ellipses for added variables, rounded rectangles for functions

-- when the symbol vis view open or cleaned, read the project files (source files only),
and keep a snapshot of each source file. once a change to a file is detected compare it
with the existing snapshot to find the changes.